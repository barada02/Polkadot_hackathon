{
  "name": "polkaflow-backend",
  "version": "1.0.0",
  "description": "PolkaFlow Backend - Intelligent Multi-Chain Polkadot Gateway API",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npm run setup-chains && npm run generate-descriptors",
    "setup-chains": "npx papi add westend2 -n westend2 && npx papi add westend2_asset_hub -n westend2_asset_hub && npx papi add westend2_bridge_hub -n westend2_bridge_hub && npx papi add westend2_collectives -n westend2_collectives && npx papi add westend2_coretime -n westend2_coretime && npx papi add westend2_people -n westend2_people",
    "generate-descriptors": "npx papi",
    "postinstall": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@polkadot-api/descriptors": "file:.papi\\descriptors",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "helmet": "^7.1.0",
    "morgan": "^1.10.0",
    "polkadot-api": "^1.20.6"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "keywords": [
    "polkadot",
    "blockchain",
    "multi-chain",
    "api",
    "express"
  ],
  "author": "PolkaFlow Team",
  "license": "MIT"
}
