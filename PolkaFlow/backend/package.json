{
  "name": "polkaflow-backend",
  "version": "1.0.0",
  "description": "PolkaFlow Backend - Intelligent Multi-Chain Polkadot Gateway API",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npm run clean-papi && npm run setup-westend && npm run generate-descriptors",
    "clean-papi": "node -e \"require('fs').rmSync('.papi', {recursive: true, force: true})\"",
    "setup-westend": "npx papi add westend2 -n westend2",
    "generate-descriptors": "npx papi",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@polkadot-api/descriptors": "file:.papi\\descriptors",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "helmet": "^7.1.0",
    "morgan": "^1.10.0",
    "polkadot-api": "^1.20.6"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "keywords": [
    "polkadot",
    "blockchain",
    "multi-chain",
    "api",
    "express"
  ],
  "author": "PolkaFlow Team",
  "license": "MIT"
}
